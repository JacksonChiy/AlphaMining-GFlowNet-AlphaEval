# Experiments

Each run creates `experiments/<experiment_id>/` containing a frozen `config.yaml`,
factor metadata, model metrics, predictions, and (after RQAlphaPlus is run) a
backtest report. Generated experiments are intentionally not committed.

