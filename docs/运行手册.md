# AlphaMining 日频复现运行手册

## 1. 适用范围

本手册用于从零运行日频版 GFlowNet + AlphaEval + LightGBM + RQAlphaPlus 流水线。行情数据只从本地 `data/price.csv` 读取，不调用外部数据接口。

完整流程如下：

```text
price.csv
  ↓
数据检查、复权、异常处理与截面标准化
  ↓
表达式引擎与 GFlowNet 因子挖掘
  ↓
AlphaEval 评价与 DPP 去相关筛选
  ↓
LightGBM 滚动融合，预测 t+1 到 t+5 收益
  ↓
RQAlphaPlus Top N 等权策略回测
```

## 2. 运行环境分工

| 阶段 | 推荐环境 | 必要条件 |
|---|---|---|
| 数据准备、表达式、GFlowNet、AlphaEval、LightGBM | Google Colab | NVIDIA A100、CUDA、PyTorch |
| 单元测试与结果检查 | 本地电脑或 Colab | Python 3.10 及以上 |
| 策略回测 | 本地已获授权的 RQAlphaPlus 环境 | Colab 产物、`rqalpha_plus` 与本地数据包 |

RQAlphaPlus 不在 `requirements.txt` 中，因为它是授权软件，必须通过米筐授权渠道单独安装。

## 3. 获取代码

在本地终端或 Colab 中执行：

```bash
git clone https://github.com/JacksonChiy/AlphaMining-GFlowNet-AlphaEval.git
cd AlphaMining-GFlowNet-AlphaEval
```

Notebook 默认使用上述地址。如果需要运行其他 fork 或分支，可在执行 clone 单元格前设置：

```python
import os
os.environ["ALPHAMINING_REPO_URL"] = "你的仓库地址"
```

### 3.1 推荐的两段式运行方式

Colab 只运行一个总 Notebook：

```text
notebooks/00_colab_full_pipeline_A100.ipynb
```

它完成数据预处理、表达式检查、GFlowNet、AlphaEval 和 LightGBM，并自动下载：

```text
alphamining_colab_outputs.zip
```

然后把压缩包放到本地仓库根目录，运行：

```text
notebooks/06_rqalpha_backtest.ipynb
```

RQAlphaPlus 全程只在本地授权环境运行。原来的 `01` 至 `05` Notebook 继续保留，仅用于分阶段调试。

## 4. 本地环境安装

### 4.1 macOS 或 Linux

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
```

macOS 如果 LightGBM 提示找不到 `libomp`，执行：

```bash
brew install libomp
```

### 4.2 安装验证

```bash
python -c "import torch, lightgbm, pandas; print('核心依赖安装成功')"
pytest -q
```

单元测试全部通过后，再开始正式实验。

## 5. 准备 `price.csv`

### 5.1 文件位置

本地直接运行时，将私有行情文件复制到：

```text
data/price.csv
```

该文件已被 `.gitignore` 忽略，不会进入 Git 提交。

使用一体化 Colab Notebook 时，请将行情文件保存到 Google Drive：

```text
MyDrive/price.csv
```

Notebook 会挂载 `/content/drive`，并在 Colab 本地文件不存在或文件大小不一致时，将它复制到 `data/price.csv`。训练读取 Colab 本地副本，避免反复从 Drive 读取大型 CSV。

### 5.2 字段要求

| 规范字段 | 必需 | 常见可识别别名示例 | 说明 |
|---|---:|---|---|
| `date` | 是 | `trade_date`、`datetime`、`日期`、`交易日期` | 交易日期 |
| `code` | 是 | `symbol`、`ticker`、`证券代码`、`股票代码` | 股票标识 |
| `open` | 是 | `open_price`、`开盘价` | 开盘价 |
| `high` | 是 | `high_price`、`最高价` | 最高价 |
| `low` | 是 | `low_price`、`最低价` | 最低价 |
| `close` | 是 | `close_price`、`收盘价` | 收盘价 |
| `volume` | 是 | `vol`、`trade_volume`、`成交量` | 成交量 |
| `amount` | 否 | `turnover`、`trade_amount`、`成交额` | 成交额 |
| `vwap` | 否 | `avg_price`、`average_price`、`成交均价` | VWAP；缺失时自动估算 |
| `adj_factor` | 否 | `adjust_factor`、`factor`、`复权因子` | 时点可得的复权因子 |
| `industry` | 否 | `industry_code`、`申万行业`、`行业` | 行业风险暴露 |
| `market_cap` | 否 | `total_market_cap`、`float_market_cap`、`市值` | 市值风险暴露 |

最小示例：

```csv
date,code,open,high,low,close,volume,amount
2020-01-02,000001.SZ,16.65,16.95,16.55,16.87,153023500,2578400000
2020-01-03,000001.SZ,16.94,17.31,16.92,17.18,111619400,1911550000
```

### 5.3 数据边界

- 每一行应代表一只股票的一个交易日；
- 同一 `date + code` 若重复，保留最后一行；
- 价格必须为正，且 `low <= high`；
- 缺失价格只在同一股票内部前向填充；
- `adj_factor` 必须是当时已经可得的数据，不能使用事后修订信息；
- 行业和市值字段缺失时可以运行，但对应风险惩罚不会启用；
- LightGBM 默认至少需要约 504 个训练交易日，建议准备明显更长的历史区间。

## 6. 阶段一：数据预处理

一体化 Notebook 会自动运行本阶段。仅需单独调试数据时打开：

```text
notebooks/01_data_prepare.ipynb
```

也可在项目根目录单独执行：

```bash
python -c "from src.data_loader import prepare_price_csv; prepare_price_csv()"
```

处理内容包括：日期转换、字段自动映射、稳定排序、去重、缺失值与异常值处理、可选复权、同日缩尾和截面 z-score。

成功后检查：

```text
data/daily_price.pkl
results/data_quality_report.json
```

重点查看质量报告中的输入/输出行数、字段映射、删除的无效行、异常值截断数量、是否执行复权，以及行业和市值字段是否可用。

## 7. 阶段二：表达式引擎检查

一体化 Notebook 会自动运行本阶段。仅需单独调试表达式时运行：

```text
notebooks/02_expression_engine.ipynb
```

该 Notebook 会随机生成表达式，完成序列化与反序列化，并在 `daily_price.pkl` 上执行因子计算。典型表达式为：

```text
cs_rank(ts_mean(close,20))
```

若此阶段出现大量空值，先确认所选时间窗口之前存在足够历史数据；窗口初期出现空值属于正常现象。

## 8. 阶段三：在 Colab A100 训练 GFlowNet

### 8.1 选择 A100

在 Google Colab 菜单中选择“运行时 → 更改运行时类型”，将硬件加速器设置为 **A100 GPU**。若账号当前无法分配 A100，请稍后重试或使用支持 A100 的 Colab 方案；正式实验不要跳过硬件校验。

### 8.2 从 Google Drive 复制数据

先把 `price.csv` 保存到 Google Drive：

```text
MyDrive/price.csv
```

运行 Notebook 的 Drive 单元格并授权挂载。代码会比较 Drive 文件和 `data/price.csv` 的字节数；仅在本地副本不存在或大小发生变化时执行复制，并打印本地 CSV 大小。Colab 临时磁盘会随会话释放，重新建立会话后需要再次复制；Drive 中的原文件不会受影响。

### 8.3 运行一体化训练 Notebook

打开：

```text
notebooks/00_colab_full_pipeline_A100.ipynb
```

按顺序运行全部单元格。该 Notebook 会连续完成阶段一至阶段五。`03_train_gflownet_A100.ipynb` 仅用于单独调试 GFlowNet。GPU 检查单元格必须输出：

- `CUDA available: True`；
- GPU 型号包含 `A100`；
- GPU 总显存；
- CUDA 与 PyTorch 版本。

默认训练参数来自 `configs/training_config.yaml`：

| 参数 | 默认值 |
|---|---:|
| 隐藏维度 | 256 |
| Transformer 层数 | 4 |
| 注意力头数 | 8 |
| 训练轮数 | 100 |
| 每轮轨迹数 | 32 |
| 学习率 | 0.0001 |
| 混合精度 | 开启 |
| 随机种子 | 42 |

训练时每条轨迹都会立即打印：

- 当前 epoch、当前 step、全局 step 与总体完成百分比；
- 生成的完整表达式及动作数量；
- reward、RankIC、LongIR 与风险惩罚；
- `logPF` 和该轨迹的 Trajectory Balance loss；
- 单轨迹耗时与训练累计耗时。

每个 epoch 完成反向传播和参数更新后还会打印：

- 当前 epoch 与总 epoch；
- Trajectory Balance loss；
- 平均奖励、最高奖励和平均 RankIC；
- `logZ`、梯度范数和学习率；
- 单轮耗时与累计耗时；
- 最佳 checkpoint 是否保存；
- A100 已分配显存、保留显存和单轮峰值显存。

逐轨迹明细保存在 `results/gflownet_trajectory_metrics.csv`，逐 epoch 汇总保存在 `results/gflownet_training_metrics.csv`，便于训练后绘图分析。训练完成后的因子池生成阶段也会为每次采样输出 attempt、接受或重复状态、当前唯一表达式数量、reward 和表达式。

正式训练会保存并重新加载检查点。成功标志：

```text
checkpoints/gflownet_best.pt
results/gflownet_training_metrics.csv
results/gflownet_trajectory_metrics.csv
results/alpha_pool.csv
results/alpha_factor_matrix.pkl
```

`alpha_pool.csv` 记录 `factor_001`、`factor_002` 等因子名称、表达式和奖励信息；`alpha_factor_matrix.pkl` 保存按日期和股票索引的因子值。

## 9. 阶段四：AlphaEval 筛选

一体化 Notebook 会自动运行本阶段。仅需单独调试 AlphaEval 时运行：

```text
notebooks/04_alpha_eval.ipynb
```

评价包括预测能力、滚动稳定性、扰动鲁棒性、表达式逻辑性、RRE 和 DPP 多样性筛选。未来收益标签严格定义为：

```text
close(t+5) / close(t+1) - 1
```

该定义表示使用 `t` 日收盘前可形成的因子，预测从下一交易日到第 5 个未来交易日的收益区间，避免把 `t` 日收盘价直接当作可成交起点。

成功后检查：

```text
results/alpha_eval_result.csv
```

重点字段包括 `factor`、`IC`、`RankIC`、`ICIR`、`Sharpe`、`complexity`、`score` 和 `dpp_selected`。后续 LightGBM 默认只使用 DPP 选中的因子。

## 10. 阶段五：LightGBM 因子融合

一体化 Notebook 会自动运行本阶段。仅需单独调试 LightGBM 时运行：

```text
notebooks/05_lgbm_model.ipynb
```

默认配置使用 756 个交易日滚动训练窗、至少 504 个训练日、每 20 个交易日重训一次，并在训练与预测之间保留 5 个交易日 purge 间隔。

成功后检查：

```text
results/lightgbm/lgbm_model.joblib
results/lightgbm/model_metrics.csv
results/lightgbm/feature_importance.csv
results/lightgbm/prediction_score.csv
```

`prediction_score.csv` 是回测输入，包含信号日期、股票代码、预测分数和当日截面排名。

阶段五完成后，一体化 Notebook 会把以下内容打包为 `/content/alphamining_colab_outputs.zip` 并触发浏览器下载：GFlowNet 检查点、因子池、因子矩阵、AlphaEval 结果、LightGBM 模型、训练指标及 `prediction_score.csv`。

## 11. 一条命令运行前五阶段

在 A100 环境、数据和依赖均准备完毕后，可执行：

```bash
python -m scripts.run_daily_pipeline --pool-size 100
```

正式运行默认强制检测 A100。仅做很小规模的本地代码路径检查时可以使用：

```bash
python -m scripts.run_daily_pipeline --pool-size 5 --allow-non-a100
```

该选项不适用于正式训练或研究结果发布。

## 12. 阶段六：本地 RQAlphaPlus 回测

### 12.1 前提

本阶段不在 Colab 运行。在本地已获授权的环境中安装 `rqalpha_plus`，准备好本地数据包，并把 Colab 下载的 `alphamining_colab_outputs.zip` 放到仓库根目录。本项目不会自动下载 RQAlphaPlus 数据包。

### 12.2 导入 Colab 产物

推荐打开并运行：

```text
notebooks/06_rqalpha_backtest.ipynb
```

Notebook 会把压缩包解压到原有目录结构，并校验 `results/lightgbm/prediction_score.csv`。压缩包同时保留原始因子池、因子矩阵和模型，便于本地审计；RQAlphaPlus 策略直接读取的是因子经 LightGBM 融合后的预测分数。

### 12.3 执行

```bash
python -m rqalpha_strategy.run_backtest \
  --bundle ~/.rqalpha-plus/bundle \
  --predictions results/lightgbm/prediction_score.csv \
  --output-dir results/backtest_report \
  --initial-cash 1000000 \
  --benchmark 000300.XSHG \
  --top-n 30 \
  --rebalance-days 5 \
  --slippage 0.001
```

### 12.4 防止未来数据泄漏

策略只选择 `signal_date < trade_date` 的最近信号。同日收盘数据生成的分数不会在同一根日线成交。RQAlphaPlus 负责 T+1、整手约束、涨跌停、成交量限制、佣金、印花税、滑点及成交记录。

### 12.5 回测输出

成功后，`results/backtest_report/` 至少应包含：

```text
backtest_result.pkl
backtest_summary.json
equity_curve.png
```

RQAlphaPlus 分析器还会生成可用的绩效报告、持仓和交易明细。重点核对年度收益、总收益、Sharpe、最大回撤、波动率、换手率、净值曲线与交易历史。

## 13. 实验记录

默认配置文件为：

```text
configs/training_config.yaml
```

每次正式实验都应使用唯一的 `experiment_id`，并在 `experiments/<experiment_id>/` 中保存：

```text
config.yaml
factor_results.csv
model_metrics.csv
prediction_score.csv
backtest_report/
```

不要覆盖旧实验。修改参数后应创建新的实验目录，以保证配置、检查点和结果可追溯。

## 14. 完整产物检查表

| 阶段 | 应生成的文件 |
|---|---|
| 数据预处理 | `data/daily_price.pkl`、`results/data_quality_report.json` |
| GFlowNet | `checkpoints/gflownet_best.pt`、`results/gflownet_training_metrics.csv`、`results/gflownet_trajectory_metrics.csv`、`results/alpha_pool.csv`、`results/alpha_factor_matrix.pkl` |
| AlphaEval | `results/alpha_eval_result.csv` |
| LightGBM | `results/lightgbm/lgbm_model.joblib`、`model_metrics.csv`、`feature_importance.csv`、`prediction_score.csv` |
| Colab 交付包 | `alphamining_colab_outputs.zip`，供本地导入训练模型、因子与融合分数 |
| RQAlphaPlus | `results/backtest_report/` 下的汇总、净值曲线和交易记录 |
| 实验归档 | `experiments/<experiment_id>/` 下的冻结配置和结果副本 |

数据、结果和检查点默认被 Git 忽略。需要公开模型时使用 GitHub Release 或 Git LFS，绝不能上传无授权的原始行情。

## 15. 常见问题

### 15.1 提示没有找到 `data/price.csv`

本地运行时确认文件路径严格为 `data/price.csv`。Colab 运行时确认 Google Drive 中存在 `MyDrive/price.csv`，并重新执行 Drive 挂载与复制单元格。

### 15.2 提示缺少必需字段

打开 `price.csv` 第一行，检查日期、代码、开高低收和成交量字段是否存在。若字段名不在自动别名表中，可先改为规范名称；不要把索引列误当作股票代码。

### 15.3 CUDA 不可用或 GPU 不是 A100

在 Colab 重新选择 A100 运行时，然后从 GPU 检查单元格重新执行。若无法分配 A100，请不要用 `--allow-non-a100` 生成正式结果。

### 15.4 macOS 导入 LightGBM 失败

先执行 `brew install libomp`，重启终端并重新激活虚拟环境。仍失败时重新安装 LightGBM wheel。

### 15.5 AlphaEval 或 LightGBM 可用样本过少

确认股票数量足够、历史长度明显超过 504 个交易日，并检查大窗口因子是否产生过多空值。行业、市值字段缺失不会阻止运行。

### 15.6 RQAlphaPlus 无法导入

确认当前运行的是本地 Notebook，而不是 Colab。这不是公开 `pip` 依赖缺失；请切换到已通过米筐授权渠道安装 `rqalpha_plus` 的本地 Python 环境。

### 15.7 没有找到 Colab 训练产物

确认浏览器已完成下载，并把 `alphamining_colab_outputs.zip` 放到本地仓库根目录。不要更改压缩包内部的 `results/` 和 `checkpoints/` 目录结构。

### 15.8 回测没有交易

检查预测文件是否包含 `signal_date`、`code`、`prediction_score`，日期是否覆盖 RQAlphaPlus 数据包，股票代码能否映射为 `.XSHG`、`.XSHE` 或 `.XBSE`，以及第一天没有更早信号是否属于预期。

## 16. 正式实验前核对

- [ ] `price.csv` 来源、复权方式和字段含义已经记录；
- [ ] 没有使用后向填充或事后修订风险字段；
- [ ] Colab 输出确认 GPU 为 A100、CUDA 可用、显存正常；
- [ ] GFlowNet 开启 PyTorch 混合精度训练；
- [ ] 标签为 `close(t+5) / close(t+1) - 1`；
- [ ] LightGBM 滚动训练折使用 5 日 purge；
- [ ] RQAlphaPlus 仅使用严格滞后的信号；
- [ ] 最终评价期未参与因子挖掘、筛选或调参；
- [ ] 所有结果与冻结配置绑定到唯一 `experiment_id`；
- [ ] 原始数据、私有数据包和未审核模型未提交到 GitHub。
