# 项目交接记忆

## 项目位置

- 项目：AlphaMining-GFlowNet-AlphaEval
- 本地路径：`/Users/jacksonchi/Documents/Quant 2/AlphaMining-GFlowNet-AlphaEval`
- GitHub：<https://github.com/JacksonChiy/AlphaMining-GFlowNet-AlphaEval>
- 网络代理：`http://127.0.0.1:7890`
- GitHub 账号：JacksonChiy，`gh` 已登录

## 项目目标

复现日频版完整流水线：

```text
GFlowNet Alpha Mining
→ AlphaEval
→ LightGBM
→ 本地 RQAlphaPlus 回测
```

Google Colab 只负责数据处理、模型训练、因子评价和预测；RQAlphaPlus 回测在本地运行。

## 研究区间与标签

- 训练和因子筛选：2020–2023 年
- 样本外预测和回测：2024–2026 年
- 未来收益标签：`close(t+5) / close(t+1) - 1`

## 数据约定

- 从 Google Drive 的 `/content/drive/MyDrive/price.csv` 复制到 Colab 的 `data/price.csv`
- 不使用外部行情接口
- 快速模式选择约 800 只股票
- 因子覆盖率门槛默认 80%
- 低覆盖表达式会受到 Reward 惩罚，并在进入 AlphaEval 和 LightGBM 前被过滤

## 已完成功能

- 一体化 Colab Notebook：`notebooks/00_colab_full_pipeline_A100.ipynb`
- GFlowNet A100 混合精度训练和详细训练日志
- 重复子表达式缓存
- Pandas 时序算子迁移至 PyTorch GPU
- 样本内表达式在 2020–2026 完整行情上重新计算
- 生成 2024–2026 年样本外因子值
- AlphaEval 向量化加速和详细进度日志
- LightGBM 滚动预测
- 本地 RQAlphaPlus 策略与运行说明
- 项目文档中文化

## AlphaEval 最新优化

- IC/RankIC 使用 NumPy 向量化聚合
- 缓存未来收益截面排名
- 向量化 Top 组合、扰动鲁棒性和排名稳定性
- DPP 按交易日均匀抽样并复用相关矩阵
- 快速配置 DPP 最多使用 300,000 行
- 正式配置 DPP 最多使用 500,000 行
- 输出逐因子四阶段耗时、总进度和预计剩余时间
- 24 万行基准中，IC/RankIC 约加速 3.4–3.5 倍，排名稳定性约加速 1.9 倍
- 测试结果：`46 passed, 1 skipped`

## Git 状态

- AlphaEval 优化已合并到远端 `main`
- 最新提交：`9567498 feat: optimize AlphaEval speed and progress logging`
- 远端 `main` 和 `agent/optimize-alphaeval` 均指向 `9567498`
- 当前本地分支：`agent/optimize-alphaeval`

## 本地未提交内容

以下内容属于用户现有修改，不得擅自覆盖、删除或上传：

- `configs/training_config.yaml`：回测初始资金从 100 万改为 1,000 万
- `notebooks/06_rqalpha_backtest.ipynb`：本地已删除
- `alphamining_colab_outputs/`：未跟踪的 Colab 训练产物

## 后续操作约束

1. 修改前先执行 `git status` 并检查差异。
2. 只暂存当前任务相关文件，禁止直接使用 `git add -A`。
3. 不得覆盖用户未提交的配置和文件状态。
4. GitHub 网络操作使用 `127.0.0.1:7890` 代理。
5. Colab 训练产物和大型行情文件不得提交到 Git。
